---
description: How to start the preview server for the project
---

# Preview Project

This workflow starts the Google Cloud Functions framework HTTP server locally on port 5000.

1. Run the local preview server:
// turbo
```bash
uv run functions-framework --target ejecutar_rastreo_minvu_extra --port 5000
```
