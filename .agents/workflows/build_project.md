---
description: How to install dependencies and build the project
---

# Build Project

This workflow installs all dependencies and prepares the Python environment.

1. Ensure `uv` is installed, then sync the dependencies:
// turbo
```bash
uv sync
```
